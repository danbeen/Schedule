// Copy this file to config.js and fill in your Supabase project URL and anon key.
// Then add <script src="js/config.js"></script> before sync.js in child.html and parent.html.
// If you don't use config.js, the app works with localStorage only (no remote sync).

window.MORNING_V2_SUPABASE = {
  url: 'https://YOUR_PROJECT_REF.supabase.co',
  anonKey: 'YOUR_ANON_KEY'
};
